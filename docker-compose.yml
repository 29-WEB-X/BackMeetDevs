# Versión de syntax archivo
version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8000:8000'
    volumes:
      - .:/api
    command: ['npm', 'run', 'dev']

  mongo:
    image: mongo:6.0
    ports:
      - '27017:27017'
